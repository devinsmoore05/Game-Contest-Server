<!DOCTYPE html>
<html>
<head>
  <title>GameContestServer</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  
  
  <nav class="navbar navbar-default" role="navigation">
    
    <div class="navbar-header"><%= link_to 'Game Contest Server', root_path, class: "navbar-brand" %></div>
    <ul class="nav navbar-nav">
      <li<% if current_page?(root_path) then 'class=active' end %>><%= link_to 'Home', root_path %></li>
      <li><%= link_to 'Users', users_path %></li>
      
      <%if !logged_in? %>
        <li><%= link_to 'Log In', login_path %></li>
        <li><%= link_to 'Sign Up', signup_path %></li>
      
      <%else%>      
      <li><%= link_to 'Profile', user_path(current_user) %></li>
      <li><%= link_to 'Settings', edit_user_path(current_user) %></li>
      <li><%= link_to 'Log Out', logout_path, method: :delete %></li>
      <%end%>

    </ul>
    <% flash.each do |level, message| %>
    <div class="alert alert-<%= level %>"><%= message %></div>
    <% end %>
  </nav>
  
<%= yield %>

</body>
</html>
